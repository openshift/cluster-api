apiVersion: v1
kind: ConfigMap
metadata:
  name: exclude-resources-config
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  excludeCRDs: |
    clusterclasses.cluster.x-k8s.io
    machinepools.cluster.x-k8s.io
    extensionconfigs.runtime.cluster.x-k8s.io

