apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: k8s.gcr.io/cluster-api/cluster-api-controller:v1.0.0
        name: manager
