apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: system
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: ocp-capi-webhook-cert
spec:
  ports:
    - port: 443
      name: https
      targetPort: webhook-server
